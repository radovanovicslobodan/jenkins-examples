pipeline {
    agent {
        label 'docker-agent-node'
    }

    environment {
        GITHUB_TOKEN = credentials('github-token')  // loads secret text
    }

    stages {
        stage('Hello') {
            steps {
                echo "Hello World!!! ${GITHUB_TOKEN.take(5)}"
            }
        }
    }
}
